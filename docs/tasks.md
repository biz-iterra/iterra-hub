# タスク台帳

iterra-hub の作業の**正本**。運用ルールは `docs/task-management.md`。

- ID は連番で再利用しない。取り下げも行を消さずに残す
- **実施有無は「本番で使える状態か」**。ローカルで動いただけでは「未」
- 完了条件は `task-management.md` §6

最終更新: 2026-08-14

## 未完了（優先度順）

| ID | 起票日時 | タスク名 | 種別 | 優先度 | 起票元 | ステータス | 実施有無 | 根拠・記録 |
|---|---|---|---|---|---|---|---|---|
| T-0100 | 2026-08-14 | 総ざらいの「個人事業主の作成」が T-0087 に追随していなかった | 不具合 | 中 | Gate 3 | 検証中 | 未 | Gate 3 で検出。事業種別を個人事業主にすると「事業主（本人の連絡先）」が出て**姓と名が必須になる**（T-0087。同時作成のチェックは既定でオン）。総ざらいは埋めずに「作成」を押しており、**ブラウザの標準検証で送信が止まってトーストも JS エラーも出ないまま画面が変わらない**。原因の分からない 20 秒タイムアウトになる。総ざらい側に姓名の入力を足し、なぜ必要かをコメントに残した。**画面の実装は正しい**（標準検証の文言が利用者には出る）。再実行で緑 |
| T-0101 | 2026-08-14 | `deal_activity_emails` の RLS が全操作 `true` だった | 不具合 | 中 | 自主 | 検証中 | 未 | 全体監査で検出。ディールの活動に紐づくメール（差出人・宛先・件名・本文）が**認証さえ通れば誰でも読み書き削除できた**。親の `deal_activities` は絞ってあるのにこちらだけ素通しで、見られないディールのメールが読める。`20260814100003` で親準拠へ（参照・追加は `is_deal_accessible`、更新は記録者本人か admin、削除は admin） |
| T-0102 | 2026-08-14 | ディール作成時の初期履歴が DB 関数側に残っている | 改善 | 低 | 自主 | 未着手 | 未 | T-0095 の作業中に判明。ステージ・ステータスの履歴は**更新の分だけ**トリガーへ寄せた（`20260814100002`）。作成時の初期履歴は `promote_lead_to_deal` と `create_deal_with_lead` が今も自前で INSERT している。どちらも同じ関数の中なのでトランザクションは切れておらず実害は無いが、記録の入口が 2 つある。**INSERT ポリシーを消し切れないのはこのため**（消したら昇格と新規作成が丸ごと落ちた。E2E-03 / 04 / 12 / 17 / 18 で確認）。AFTER INSERT トリガーへ寄せるなら、既存関数 4 本から INSERT を外して二重記録にならないことを確かめる必要がある |
| T-0103 | 2026-08-14 | 連携プロファイルの保存が upsert で楽観ロックを載せられない | 改善 | 低 | 自主 | 未着手 | 未 | T-0096 の作業中に判明。`saveCompanyIntegrationProfile` は `company_integration_profiles` を `upsert` している。行が無ければ作る性質上、`WHERE updated_at = ...` を足すと**初回保存が必ず 0 行になって失敗する**。update と insert に分ければ載せられるが、プロファイルは「値ではなくレコードを選ぶ」設計の中核（T-0060）なので、触るなら E2E-16 と併せて確認する。競合するのは「2 人が同時に同じ事業者の担当者メールを選び直す」場合に限られ、失われるのも選択 1 つ |
| T-0104 | 2026-08-14 | `updateEntityAddress` が addresses と entity_addresses を別々に更新している | 改善 | 低 | 自主 | 未着手 | 未 | T-0096 の作業中に判明。住所本体（`addresses`）と紐付け（`entity_addresses`）を別文で UPDATE している。片方だけ通ると住所は変わったのにラベル・電話が古いままになる。T-0094 / T-0095 と同型で、DB 関数へ寄せるのが筋。**追加の `addEntityAddress` は既に 1 トランザクションの DB 関数**になっているので、更新側だけが残っている |
| T-0094 | 2026-08-14 | リード作成が `leads` と `lead_owners` を別々に INSERT している | 不具合 | 中 | 自主 | 検証中 | 未 | 全体監査で検出。`createLead` はリードを INSERT した後に副担当を別文で INSERT する。supabase-js は複数文を 1 トランザクションにできないため、**副担当の INSERT だけ失敗するとリードはできて副担当が付かない**中途半端な状態が残る。エラーは出るが巻き戻らない。CLAUDE.md「複数テーブルへの書き込みは DB 関数にまとめる」に反する。`promote_lead_to_deal` と同じく PL/pgSQL 関数へ寄せる **対応:** `create_lead_with_owners`（`20260814100001`）へ寄せた。**副担当の INSERT が期待件数に満たなければ例外にしてリードごと巻き戻す**（RLS で弾かれると 0 行になりエラーも出ないのが元の症状）。値の整形は TS 側のまま。回帰は E2E-20。ローカルで E2E-02 / 03 / 12 / 20 が緑 |
| T-0095 | 2026-08-14 | ディールの更新と履歴の記録が別トランザクションになっている | 不具合 | 中 | 自主 | 検証中 | 未 | 全体監査で検出。`updateDeal` / `moveDealCard` が `deals` を更新したあとに `deal_stage_histories` / `deal_status_histories` を別文で INSERT する。**履歴の INSERT だけ失敗すると更新は残って履歴が欠ける。** ステージ滞留日数が実態とずれる。`entity_change_logs` はトリガーが拾うので変更の事実自体は残るが、滞留期間の集計はこの 2 表が正本。T-0094 と同型  **対応:** AFTER UPDATE トリガー `trg_deals_stage_status_history`（`20260814100002`）へ寄せ、アプリ側の INSERT を 2 箇所とも削除した。**アプリ経由でない更新（service_role / SQL 直接）も拾える。** 実行者は `auth.uid()`、無ければ `last_updated_by`。どちらも取れないときは**履歴を書かずに更新は通す**（`changed_by` は NOT NULL。誰がやったか分からない行を残さない）。回帰は IT-DEALHIST-01〜04。**作成時の初期履歴は DB 関数側に残っている**（T-0102） |
| T-0096 | 2026-08-14 | 楽観ロックを通していない更新系がある | 改善 | 中 | 自主 | 検証中 | 未 | 全体監査で検出。主要 7 エンティティ（取引先・事業者情報・連絡先・ディール・契約・リード・プロジェクト）は `expected_updated_at` を通しているが、**マスタ CRUD（`updateMasterRecord`）・メンバー管理・従属テーブル（連絡手段・住所・SNS・タレントのスキル／経歴／実績）・freee の連携プロファイル**は素の UPDATE。後勝ちで上書きされる。マスタは複数の admin が同じ画面を開くため実害が出やすい **対応:** マスタ CRUD（`updateMasterRecord`）・メンバー管理・連絡手段（メール／電話）・住所・SNS／チャット・タレントのスキル／経歴／実績に `expected_updated_at` を通した。**`contact_emails` / `contact_phones` は `updated_at` 列そのものが無かった**ので `20260814100004` で追加（既存行は `created_at` に揃える。触っていない行が「たった今更新された」ことにならないように）。**連携プロファイル（`company_integration_profiles`）だけ対象外。** `upsert` で書いており、行が無ければ作る性質上そのままでは WHERE 条件を足せない。update と insert に分ける設計変更が要るので分けた（T-0103） |
| T-0097 | 2026-08-14 | ディールのアクティビティが記録者本人にしか見えない | 改善 | 中 | 自主 | 検証中 | 未 | 全体監査で検出。`deal_activities` の SELECT ポリシーが `is_manager_or_above() OR performed_by = auth.uid()`。**担当を引き継いだ member には前任者の記録が見えない。** ディール本体は `owner_user_id` で見えるのに活動だけ欠ける。`lead_activities` は `is_lead_accessible(lead_id)` で親に合わせてあり、こちらだけ揃っていない。**業務上どちらが正しいかは要確認**（他人の対応内容を伏せる意図があるなら現状のままでよい）  **利用者判断でディールに合わせた。** `20260814100003` で SELECT を `is_deal_accessible(deal_id)` へ。INSERT も同じ条件に絞った（以前は `true` で、参照できないディールにも記録を足せた）。**更新は記録者本人と admin のまま**（他人の対応内容は書き換えさせない。`lead_activities` と違い編集の証跡列を持たないため範囲を広げない）。ステージ・ステータス履歴の参照も同じ条件へ揃えた |
| T-0098 | 2026-08-14 | 履歴テーブルの INSERT が誰でも通る | 改善 | 低 | 自主 | 検証中 | 未 | 全体監査で検出。`account_change_histories` / `company_change_histories` / `contact_change_histories` / `deal_change_histories` / `deal_stage_histories` / `deal_status_histories` の INSERT ポリシーが `true`。UPDATE / DELETE のポリシーが無いので後からの改ざんはできないが、**任意の内容を書き足せる**。`entity_change_logs` は INSERT ポリシーを持たずトリガー（SECURITY DEFINER）だけが書く形になっており、そちらに合わせるのが筋  **対応:** `*_change_histories` 4 表（**0 件・コードからの参照なし**。`entity_change_logs` へ置き換わった名残）は書き込み口を閉じ、参照を manager 以上へ。テーブル自体は落としていない。ディールの履歴 2 表は `is_deal_accessible(deal_id)` に絞った。**INSERT ポリシーを消し切ることはできない**（初期履歴を書く既存 DB 関数が落ちる。T-0102） |
| T-0099 | 2026-08-14 | `updateProject` に担当者チェックが無い | 改善 | 低 | 自主 | 完了 | 済 | 全体監査で検出。`projects` は `owner_user_id` を持つが、`updateProject` は manager 以上かどうかしか見ていない。**manager なら他人のプロジェクトを更新できる。** CLAUDE.md の「update 系は admin 以外はオーナー確認」と食い違う。**利用者判断で CLAUDE.md 側を直した。** プロジェクトは複数の担当が横断して動かすもので、担当者本人しか触れないと進行が止まる。実装は変えず、例外として明文化した（例外を増やすときは理由を書く、も併記） |
| T-0089 | 2026-08-14 | スクロールすると画面全体がずれる（サイドバーとヘッダーが画面外へ出る） | 不具合 | 高 | 利用者 | 検証中 | 未 | 本番で再現・原因特定済み。`.sr-only` は `position: absolute` で、`<main>` が非配置のため**包含ブロックが初期包含ブロック（`<html>`）になる**。`main` の `overflow-y: auto` にクリップされず `<html>` のスクロール領域を押し広げるため、`h-screen overflow-hidden` のはずのシェルごと文書がスクロールする。本番の `/activities` で 782px、`/dashboard` で 271px の文書スクロールを実測 **対応:** **`<main>` に `position: relative` を追加**（`app-shell.tsx`）。包含ブロックがこの要素になり、`overflow-y: auto` がクリップするようになる。併せて `APP_SCROLLER_ID` を振り、`src/lib/app-scroll.ts` にスクロール操作を集約した。**本番の DOM 上で等価の状態を作って検証済み**（`/activities` の文書スクロール 782px → 0px、サイドバーとヘッダーが固定されたままになることを目視）。CLAUDE.md に規約として追記、回帰は E2E-19。**ローカルで E2E-19 を実行して緑。`relative` を外すと落ちることも確認済み**。E2E 32 件 + 総ざらいも緑 |
| T-0090 | 2026-08-14 | 入力エラーのときに先頭へ戻らない（`window.scrollTo` が効かない） | 不具合 | 中 | 自主 | 検証中 | 未 | スクロールするのは `<main>` であって `window` ではない。連絡先の編集・タレントの編集の 2 箇所。**現在は T-0089 の文書スクロールに救われて偶然それらしく動いており、T-0089 を直すと完全な無反応になる**ので同じ作業内で直す **対応:** `scrollAppToTop()`（`src/lib/app-scroll.ts`）へ置換。E2E-19 で確認済み（**`window.scrollTo` に戻すと落ちる**ことも確認）。**姓・名は `required` が付いていてブラウザの標準検証で止まるため、サーバーまで届く入力エラー（フリガナ 51 文字以上）で見ている** |
| T-0091 | 2026-08-14 | モーダル・ドロワーを開いている間に背面がスクロールする | 不具合 | 中 | 自主 | 検証中 | 未 | `app-shell.tsx` の `document.body.style.overflow = "hidden"` は**スクローラが body ではない**ため何もしていない。モーダル側にはロック自体が無い。オーバーレイ上でホイールを回すと背面の一覧が動く **対応:** `useScrollLock`（`src/hooks/useScrollLock.ts`）を新設し、ドロワーと 7 つのモーダル（確認ダイアログ・マスタ管理・契約の紐付け・キャンペーンのリード紐付け・社内対応の編集・顧客行動の追加・昇格確認）へ適用。E2E-19 で確認済み |
| T-0092 | 2026-08-14 | 背の高いモーダルの上端が切れて操作できなくなる | 不具合 | 中 | 自主 | 検証中 | 未 | オーバーレイが `display:flex; align-items:center` の中央寄せなので、中身が画面より高いと**上へはみ出した分に到達できない**。`maxHeight` / `overflowY` が無いのは顧客行動の追加・リード編集の確認・マスタ管理の 3 つ **対応:** 顧客行動の追加・昇格確認・マスタ管理の 3 つに `maxHeight: calc(100vh - 2rem)` と `overflowY: auto` を入れた（マスタ管理はオーバーレイの `padding` も無かったので併せて追加）。E2E-19 で確認済み（画面高 560px でモーダルが画面内に収まる） |
| T-0093 | 2026-08-14 | リード詳細の sticky 右カラムが画面より高いと下端に到達できない | 不具合 | 低 | 自主 | 検証中 | 未 | 「社内対応を追加」フォームが `position: sticky; top: 1rem` で高さ上限を持たない。フォームが表示領域より高い画面では保存ボタンまでスクロールできない **対応:** `maxHeight: calc(100vh - 6rem)` と `overflowY: auto` を追加。E2E-19 で確認済み（画面高 620px で「追加する」まで届く） |
| T-0088 | 2026-08-10 | バルク系以外にも PUBLIC の EXECUTE が残る関数を棚卸しする | 改善 | 低 | 自主 | 未着手 | 未 | T-0085 の実装中に確認。`import_inquiry_leads`（呼び出しは service_role のみ）・`recalculate_lead_scores_for_batch`・`upsert_freee_partners`（service_role へ GRANT 済みだが PUBLIC の EXECUTE が残存）など。T-0085 と同じ手順（経路の洗い出し → REVOKE → 42501 と正規経路の両立を実測）で揃える。**注意: `import_eight_leads` は SECURITY INVOKER のままなので、取込ワーカーを INVOKER に変えると 42501 で止まる**（触るときの申し送り） |
| T-0048 | 2026-08-05 18:10 | マスタ管理のモーダルに `role="dialog"` が無い | 改善 | 低 | 自主 | 検証中 | 未 | 支援技術にモーダルと伝わらず、E2E でも入力欄を位置でしか指定できない（ヘッダーの検索欄を誤って掴む事故が起きた）。マスタ管理の `Modal`・共通の `ConfirmDialog`・**キャンペーンのリード紐付けモーダル**に `role="dialog"` / `aria-modal` / `aria-labelledby`（確認ダイアログは `aria-describedby` も）を付けた。`useId` で採番するので同じ画面に複数出ても衝突しない。**キャンペーンの分は E2E-11 を書いていて見つかった**（掴めないので気づく） |
| T-0046 | 2026-08-05 15:30 | Google コンタクト同期の Phase 2（Google→CRM の取り込み） | 改善 | 中 | 自主 | 未着手 | 未 | ミラー取り込み（syncToken）・差分画面・Google 側変更の確定・**初回突合**（既存の Google 連絡先とのメール一致）・グループ入り候補の承認。競合（etag 不一致）の解決もここ。Phase 1 では理由を残すだけにしてある |
| T-0044 | 2026-08-05 14:25 | freee の「名前（ふりがな）」を連携する手段が無い | 改善 | 低 | 自主 | 保留 | 未 | API のカナ系項目は `name_kana`（＝正式名称（カナ））1 つだけ。`shortcut1`/`shortcut2` は画面にも別にある別物なので流用しない。**freee の画面で人が入れるしかない**。freee 側に項目が増えたら対応する |
| T-0040 | 2026-08-05 12:30 | freee へ送信した直後に差分が消えない | 改善 | 中 | 自主 | 検証中 | 未 | `pushPartnerToFreee` が `updatePartner` の戻り値（更新後の取引先）を捨てており、ミラー `freee_partners` を更新しない。次の同期まで同じ差分が残り「反映されていない」ように見える。全項目で起きる。**戻り値で `upsert_freee_partners` を 1 件だけ呼ぶ**形に修正（`p_full` は false。true にするとこの 1 件以外が全部「freee 側で削除済み」になる）。ミラー更新だけ失敗した場合は、freee への書き込みが済んでいることを文言で分けて返す（送り直しによる二重反映を避けるため） |
| T-0036 | 2026-08-05 02:10 | freee の接続と定期同期を本番で有効にする | 運用 | 高 | 自主 | 検証中 | 未 | **環境変数 4 つ・開発者コンソールのアプリ登録・接続・初回の全件同期まで完了**（2026-08-06）。取引先 38 件を取り込み、エラーなし。残りは**タスクスケジューラ 2 本の登録**（差分＝日次・全件＝週次。`deployment-nas.md` §8.0.1）と、**差分画面での突合**（`/admin/freee/sync`）。連携項目を 9 → 20 以上へ増やしたので初回はまとまった差分が出る。**定期同期は取り込みのみで freee 側には書かない**（人が反映を選んだときだけ更新・新規登録が走る） |
| T-0055 | 2026-08-05 21:40 | Google コンタクト同期を本番で有効にする | 運用 | 高 | 自主 | 検証中 | 未 | GCP の OAuth クライアント・環境変数 5 つ・タスクスケジューラの登録まで完了（2026-08-06）。**初回の全件登録が 751 件・失敗 0 件で完了**。所要は 150 件あたり約 11 分で、差はすべて People API の上限待ち（`withRetry` の 2→4→8 秒。**失敗として記録されない**）。初回だけ 30 分間隔にし、送り切ったので 1 時間へ戻す。残りは **他メンバーの `/profile` からの接続**と、**変更が Google へ流れることの確認**（連絡先を 1 件編集して応答が `updated:1` になること）。**接続した人ごとに全件 push が走る**ので、1 人ずつ送り切ってから次の人に接続してもらう |
| T-0037 | 2026-08-05 02:10 | 本番の実機確認（日本語入力を含む） | 運用 | 高 | 自主 | 未着手 | 未 | **IME の変換確定は自動テストで再現できないと書いていたが、誤りだった。** CDP の `Input.imeSetComposition` で本物の変換を起こせる（E2E-14 が既にそうしていた）。住所の「市区町村」と「郵便番号」で変換確定 Enter を試す 2 ケースを足し、**フォームが送信されないこと・住所検索が走らないことを確認**（`AddressesEditor` の実装は正しく、直す必要は無かった）。残るのは**本番のデータでの目視**（事業者名/事業種別の表示、個人事業主の出し分け、ステージ規則）。ローカルでは総ざらいと E2E 12 本で確認済み |
| T-0058 | 2026-08-06 03:30 | freee の担当者名が CRM に居ないと差分を消せない | 改善 | 中 | 利用者 | 検証中 | 未 | 候補は**その事業者に関わる連絡先だけ**から探す（同名の別人を拾わないため）。freee にしか居ない担当者は候補 0 件で、freee → CRM は拒否、CRM → freee を選ぶと**freee の担当者名が消える**。**どの経路も取れず差分が残り続けた**（本番 4 件）。**連携プロファイルに `ignored_fields` を足して解決**（`20260806000005`）。項目ごとに「突き合わせない」と宣言でき、**消えるのは表示だけで値は変えない**。取引先まるごとの `excluded` では粒度が粗すぎて他の項目まで見えなくなるため採らなかった。**戻す入口は差分一覧とは別**に置いた（対象外にした項目はその一覧から消えるため） |
| T-0060 | 2026-08-06 12:00 | 連携先へ渡す値を事業者ごとに選べるようにする（連携プロファイル） | 改善 | 中 | 利用者 | 検証中 | 未 | freee の担当者メールは「主担当の主メール」で決まり、**主メールは連絡先に 1 つしか立たない**。同じ人が 2 社の主担当だと両社へ同じメールが渡り、会社ごとに使い分けていると片方が永久に差分として残る。利用者の指摘「**項目が増えるたびに列を足すのは本質的でない**」を受け、**値ではなくレコードを選ぶ**プロファイル `company_integration_profiles` を新設（`20260806000003`）。担当者・メール・住所・電話・口座の 5 つを事業者 × 連携先ごとに選べる。**NULL は既定に従う**ので既存データは無変更。参照は `resolve_company_integration_values` に集約し、`detect_freee_partner_diffs` はそれを呼ぶだけにした（`20260806000004`）。選べる範囲は DB のトリガーが縛る。E2E-16 で固定。**連携先が増えてもスキーマ変更は不要**だが、別レコードを選び分ける項目が増えると列は 1 本増える |
| T-0061 | 2026-08-06 12:00 | freee の差分画面から連携プロファイルを直せるようにする | 改善 | 低 | 自主 | 検証中 | 未 | 事業者情報の詳細まで移動しないと担当者メールを選べなかった。差分画面のメールの行に**担当者のメールから選ぶ**選択欄を置き、その場でプロファイルへ保存できるようにした。**他の列は触らない**（upsert で全列を送ると、既定に戻したつもりの無い列まで消える）。併せて各行に「この項目は突き合わせない」を置いた（T-0058） |
| T-0059 | 2026-08-06 03:30 | 同じ人が 2 社の担当者になれない（連絡先と事業者情報が N:1） | 改善 | 中 | 利用者 | 検証中 | 未 | `contacts.company_id` が 1 列なので**1 人は 1 つの事業者情報にしか属せない**。freee 突合で 2 件該当（坂本 明久、人見 麻里）。利用者に確認したところ**兼務**だった。**案 B（追加）で実装**（マイグレーション `20260806000002`）。`contacts.company_id` は「主たる所属」として残し、兼務を `company_contacts` に足す。参照はビュー `company_contact_affiliations` に寄せて**2 か所で判定させない**。freee の担当者候補と主担当の設定もビュー経由にした。連絡先の詳細に「兼務先」、事業者情報の連絡先一覧に「兼務」バッジ。E2E-15 で固定。**全面的に中間表へ移す案は採らず**（TS 33 / マイグレーション 35 / ポリシー 23 に影響）、ビューを後から移すときの受け皿にしてある |
| T-0057 | 2026-08-06 02:30 | freee の口座種別が「どちらも未設定」でも差分として出る | 不具合 | 中 | 利用者 | 検証中 | 未 | **freee は口座種別に未設定を持てない**（画面で何も選ばなくても API は `ordinary` を返す）。つまり freee 側の `ordinary` は「普通預金と決めた」ではなく「未設定、または普通預金」。CRM は NULL を取れるので素で比べると**どちらも未設定なのに差分として並び**、38 件の突合で毎回この 1 行が出て本当に直すべき差分が埋もれていた。比較の直前に両側を揃える `normalize_account_type()` を入れた（マイグレーション `20260806000001`）。**CRM のデータに `ordinary` を書き込む案は採らない**（freee の `ordinary` は情報を持たないので、それを根拠に「普通預金」と書くのは推測を事実にしてしまう）。TS 側は `normalizeAccountType`、UT-72 で固定。**入力フォームも「普通」で開くようにした**（`FinancialInfoEditor`）。未設定の既存行を開いたときも「普通」になり、**未選択の選択肢は外した**（freee に未設定が無い以上、空にできても意味が無い）。**既存データの一括書き換えはしない**。人が開いて保存したときだけ値が入る。総ざらいに「口座種別は普通で開き、未選択の選択肢が無い」を追加。**ただし既存の NULL 行を開いたときの挙動は自動テストで押さえられていない**（画面から NULL の行を作れなくなったため。freee からの取り込みでのみ発生しうる） |
| T-0056 | 2026-08-05 23:40 | 一覧はバッジ、詳細は素のテキスト、と食い違っている | 改善 | 低 | Gate 3 | 検証中 | 未 | E2E-09 を書いていて発見。利用者判断で**バッジへ揃えた**。対象は事業者情報・連絡先・取引先（ステータスと種別）・プロジェクトの詳細ページ 4 つ。`InfoField` は `value` に ReactNode を取れるので器は据え置き、中身だけ差し替えた。**必要な `color` は 4 つとも既に SELECT されていた**。E2E-09 に詳細ページの色の確認を戻し、`database-design.md §16.2` に決定を残した |
| T-0029 | 2026-08-04 17:05 | 事業者情報のフォームのラベルが「会社名」のまま | 改善 | 低 | 自主 | 検証中 | 未 | 2026-08-02 に「事業者情報」へ改称したが、**一覧の列見出しと検索欄が「会社名」のまま**だった。実際に引いているのは `companies.name`（事業者名）で、`corporate_name`（会社名）とは別の列。取引先一覧の `company.name` も同じ誤り。3 箇所を「事業者名」へ直し、検索欄は対象（事業者名・カナ・事業者コード）を書いた。E2E-04 と E2E-14 が placeholder を指しているので併せて更新。**新規作成・編集フォームの「会社名」は正しい**（別の列を指しているため据え置き） |
| T-0018 | 2026-08-04 01:30 | Zod の既定エラー文言が英語のまま出ていないか点検する | 改善 | 中 | 自主 | 検証中 | 未 | **Zod の既定文言（英語）はスキーマに `error:` を書き忘れると出る。** 個別に潰すのではなく `z.config()` で全体に受け皿を置いた（`src/lib/validators/error-map.ts`、`common.ts` から適用）。`zod/locales` の日本語をロケール側にも入れ、Zod を上げて issue が増えても英語にならないようにしてある。**未入力は型の話にせず「入力してください」に寄せた**。UT-71（12 ケース）で判定と配線の両方を固定。`error-messages.md` §2 に追記 |
| T-0004 | 2026-08-04 01:10 | 一覧の検索で打鍵直後に行を押すと詳細へ遷移しない | 不具合 | 中 | Gate 3 | 保留 | 未 | 打鍵から 300ms 以内のみ。**利用者判断で今回は記録のみ**。修正案は `test-cases/08-e2e-scenarios.md` §6.0 |
| T-0005 | 2026-08-04 01:10 | Gate 4 の残り（名刺取込・楽観ロック）を本番で確認する | 運用 | 中 | Gate 4 | 保留 | 未 | 利用者判断で本番確認に回した。T-0001 の解消待ち |
| T-0007 | 2026-08-04 01:17 | 本番の書き込み系を実機検証する | 運用 | 中 | 自主 | 未着手 | 未 | `deployment-nas.md` §7.2 #12〜17。実業務の操作に合わせる（§7.3） |
| T-0008 | 2026-08-04 01:17 | 封じ込めアカウントのログイン試行と社外回線からのアクセスを確認する | 運用 | 低 | 自主 | 未着手 | 未 | `deployment-nas.md` §7.2 #11 / #20。ログアウトと社外回線が要る |
| T-0009 | 2026-08-04 01:17 | NAS の SSH を鍵認証にする | 運用 | 中 | 自主 | 未着手 | 未 | 現在パスワード認証のまま（`deployment-nas.md` §5.0） |
| T-0010 | 2026-08-04 01:17 | 法人番号 Web-API 照合の定期実行を実装する | 改善 | 低 | 自主 | 未着手 | 未 | 実在性の状態（未確認/実在確認済/要確認/閉鎖）の自動更新が未実装 |
| T-0011 | 2026-08-04 01:17 | キャンペーンのシナリオ機能を検討する | 改善 | 低 | 利用者 | 保留 | 未 | **Lead/Campaign の土台が固まるまで着手しない**（先回り実装をしない判断） |
| T-0013 | 2026-08-04 01:17 | E2E を nightly で自動実行する | 改善 | 低 | 自主 | 未着手 | 未 | `test-strategy.md` §7 Phase 3 |
| T-0014 | 2026-08-04 01:17 | E2E-06〜11（ランク A/B）を実装する | 改善 | 低 | 自主 | 検証中 | 未 | 6 本すべて実装（spec 12 本 / テスト 25 本、5.2 分）。**仕様どおりに書けなかった 2 本は仕様の方を直した**。E2E-06 は Eight の CSV に法人番号の列が無いため名寄せを確かめられず、略記の展開と再取込で増えないことに置き換え。E2E-10 は D1 と合言葉が要りローカルで動かせないため、**同期エンドポイント 4 本が合言葉なしで動かないこと**に置き換えた（middleware の認証対象外なので、緩むとログイン不要の口が本番に開く）。書く過程で `role="dialog"` の欠落をキャンペーンの紐付けモーダルにも発見（T-0048 で対応） |

## 完了

| ID | 起票日時 | タスク名 | 種別 | 優先度 | 起票元 | ステータス | 実施有無 | 完了日時 | 根拠・記録 |
|---|---|---|---|---|---|---|---|---|---|
| T-0082 | 2026-08-09 | 横断検索（Ctrl+K）でコード（CMP- / CNT- 等）を引けるようにする | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-10 | 利用者の依頼「連絡先などを ID で検索できるようにしてください」。一覧の検索は連絡先（contact_code）・事業者情報（company_code）とも対応済みで、穴はヘッダーの横断検索（`globalSearch`）だけだった。deals / accounts / companies / contacts / contracts / projects のコード列を検索対象と副題表示に追加（leads / campaigns はコード列自体が無い） |
| T-0087 | 2026-08-09 | 個人事業主の作成時に本人連絡先を同時作成する（T-0086 再発防止） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-10 | 2026-08-09 実装（engineer/Opus が実装、Fable がレビュー・ゲート・実操作確認）。DB 関数 `create_company_with_contact`（`20260809120001`。`create_contact_with_details` を入れ子・SECURITY INVOKER・**紐づけ UPDATE の 0 行を例外化**して RLS の黙殺を防止）。ゲート 4 点 + test:db **67/67**（IT-COMPANY-CONTACT-01〜05・Q15 含む）。ブラウザ実操作で「個人事業主 → カード既定オン → 作成 → 詳細で事業主・連絡先一覧・freee 担当者に反映」「チェックオフ → 案内文」「法人 → カード非表示」を確認済み。既知の割り切り: サーバー側 Zod エラーは `[representative.姓]` 形式の内部名が出る（required によりほぼ到達しない）。計画: `~/.claude/plans/distributed-wibbling-thunder.md`。①作成フォームに既定オンのチェックボックス + 氏名欄を出し、DB 関数 `create_company_with_contact`（`create_contact_with_details` を入れ子）で単一トランザクション作成、事業主・主担当へ自動紐づけ ②整合性検査 Q15「連絡先ゼロの個人事業主」を test:db と deploy-precheck に追加。**必須にはしない**（氏名不明時に仮名を入れて通す運用に化けるため。外した分は Q15 が検出）。スコープは個人事業主のみ、法人は contact_type 差し替えで届く形にだけしておく |
| T-0085 | 2026-08-09 | バルク系 DB 関数を `.rpc()` の直接呼び出しから塞ぐ | 改善 | 低 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-10 実装（engineer/Opus）。`20260810100002` で 3 関数の EXECUTE を PUBLIC / anon / authenticated から REVOKE し service_role に限定。**塞ぐ前に呼び出し経路 9 本を洗い出し**、pg_cron ワーカー（SECURITY DEFINER・所有者 postgres）・週次 cron・service_role スクリプトの正規経路がすべて無傷であることをローカルで実測（authenticated からは 42501、manager の全件検出・ジョブ実行は成功）。src からの `.rpc()` 直呼びは 0 件。1 件版（`recalculate_lead_score` 等）と自前で権限判定を持つ入口は意図的に対象外。**同類の残り（`import_inquiry_leads` ほか）は T-0088 に起票**。test:db に IT-BULK-GRANT-01〜03。記録: `database-design.md §27.5` |
| T-0084 | 2026-08-09 | SNS・チャットの一意制約が workspace NULL のとき効かない | 不具合 | 中 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-10 実装（engineer/Opus）。`20260810100001` で `UNIQUE NULLS NOT DISTINCT` へ張り替え（PostgreSQL 15 未満なら DO ブロックが明示的に停止する）。**掃除を制約より先に**行い、最古 1 行を残して物理削除（`COALESCE(workspace,'')` は空文字と混ざるため使わず、PARTITION BY の NULL 等価性を利用）。削除はトリガーが `entity_change_logs` に残す。ローカルの対象は 0 件、合成データ 5 行で NOTICE と挙動を実証。制約名据え置きなので既存の 23505 文言がそのまま効く。test:db に IT-SOCIAL-UNIQUE-01〜04（74/74 PASS）。**デプロイ前に本番の重複件数を SQL で把握すること**（掃除は物理削除のため）。フォーム内の同一行重複はクライアントで弾いておらず DB 頼み（UX 改善余地として記録のみ）。記録: `database-design.md §25.1.2` |
| T-0022 | 2026-08-04 12:40 | 操作マニュアル画面（`/manual`）に Gmail 連携と freee 連携を反映する | 改善 | 低 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-09 実装。既存 11 章はそのまま、末尾に「12. 外部連携」を新設（12.1 Gmail / 12.2 Google コンタクト / 12.3 freee 会計。差分の確認と反映・register・連携プロファイルを含む）。`operation-manual.md` に記載の無い画面はソースを読んで記述し、§8.5 と矛盾しないことを確認済み。**同期の仕組み化は未着手のまま**（実装とマニュアルが別ファイルである根本は残る。個別追記で当面は回す判断）。**2026-08-10 に `docs/operation-manual.md` 側も追随**（engineer/Opus が改稿、Fable がレビュー・追補）: 誤り 4 件（freee 読み取りのみ／取引先必須／リード紐付けない／代表者紐付けない）を訂正、§14 外部連携を新設、§6.3 契約を全面改稿、§3.1 サイドバー表・§8.1 タブ 23 個・§12.5 ジョブ方式・ディール削除（admin 限定・90 日物理削除）も反映。あわせて実装側の取り残し 1 件を修正（リード新規作成のラベル「カテゴリ」→「デマンドファネル」。T-0077 の漏れ） |
| T-0026 | 2026-08-04 15:10 | 連絡先の新規作成に SNS・チャットも載せる | 改善 | 低 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-09 実装。`create_contact_with_details` に `p_social_accounts` を追加（`20260809110001`。引数が増えると別オーバーロードになるため旧 5 引数版を DROP してから再作成）。下書き UI は編集画面の `SocialAccountsEditor` と同じサービス連動（workspace は `requires_workspace` のときだけ）。重複時は契約どおり全体ロールバック（孤児 contacts 行が残らないことを RPC 実測で確認）。**発見した既存不具合は T-0084 に起票**。CNT-41 / `database-design.md §25.1.1` |
| T-0020 | 2026-08-04 02:05 | 統合候補の一括検出と全件スコア再計算もジョブ方式に寄せる | 改善 | 中 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-09 実装。T-0019 の名刺取込ジョブを踏襲し、payload を持たない 2 操作は `admin_bulk_jobs` 1 表を `job_type` で共有（`20260809100001`）。pg_cron が毎分 `process_admin_bulk_jobs()` を起動し `SKIP LOCKED` で 1 件ずつ処理。ワーカーは権限判定を持たない `record_contact_merge_candidates(NULL)` / `recalculate_all_lead_scores()` を直接呼ぶ（pg_cron に `auth.uid()` が無いため。権限は投入側の RLS + Server Action で完結）。RLS 境界（member 拒否 / manager は統合候補のみ / admin は両方）と二重処理防止をローカル実データ 3,009 件で検証済み。派生の起票: T-0085。設計は `database-design.md §27` |
| T-0012 | 2026-08-04 01:17 | 結合テストの SQL ケースを `npm run test:db` として固定化する | 改善 | 低 | 自主 | 完了 | 済 | 2026-08-10 | 2026-08-09 に第 1 陣を実装。`scripts/test-db/`（Node + `pg`、psql 非依存）で 02 の 61 ケースを移植し、ケースごと BEGIN/ROLLBACK・`SET LOCAL ROLE` によるロール偽装で 61/61 PASS（2 回実行で冪等・残骸なしを確認）。未移植は IT-09〜30（大型フィクスチャ）・IT-RLS-01〜19/22（同機構で可能、量の問題）・IT-JOB-01・IT-FREEE-01/02・IT-MASTER-07（理由は `test-strategy.md §7`）。**移植中に 02 の記述が T-0069（`deals.lead_id` 正本化）より古いことを発見し、両文書に追記**。依存追加は devDependencies の `pg` / `@types/pg` のみ |
| T-0006 | 2026-08-04 01:17 | 統合候補の一括検出を実行する | 運用 | 低 | 自主 | 完了 | 済 | 2026-08-10 | `/contacts/merge-candidates` の「候補を洗い直す」。ローカルでは 52 組。**2026-08-10 に本番でジョブ実行**（T-0020 のジョブ方式）。新規候補なし。既存候補の突合（統合するかの判断）は営業判断で継続 |
| T-0086 | 2026-08-09 | CMP-003597 の法人格が法人扱いの疑い + 個人事業主の連絡先が消えている疑い | 調査 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 本番確認中の報告「法人扱いになっているかもしれない」「個人事業主として登録されている事業者情報に紐づく連絡先が存在しない。元々あったはずが削除されてしまっているかもしれない」。本番 SQL で切り分け済み（2026-08-09）: ①法人格は**正しく「個人事業主」**（8/5 に法人格・代表者名を設定済み）②連絡先は**削除ではなく最初から未作成**（論理削除込み 0 件、`entity_change_logs` に痕跡なし。事業者は 8/4 に手入力で作成され、事業者情報の新規作成は連絡先を作らない）③連絡先ゼロの個人事業主は**全体でこの 1 件だけ**で構造問題ではない。freee 差分「担当者名 (未入力)↔佐川琴美」の原因もこれ。**修復完了（利用者が実施・本番画面で確認済み）**: 連絡先「佐川 琴美」を作成し事業主・主担当に紐づけ、住所・リードソースも整備。freee へ渡る値（氏名・メール・住所）も確認。再発防止は T-0087（実装済み・デプロイ待ち） |
| T-0083 | 2026-08-09 | 本番の CMP-003597 が freee に存在し CRM に見えない | 調査 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 本番 SQL で切り分け。**CRM に実体あり**（佐川 琴美・個人事業主・`deleted_at` NULL）で、ミラー `freee_partners` との紐づけ（company_id）も正常。見えなかった原因は**ヘッダーの横断検索がコード非対応**だったこと（T-0082 で解消）。一覧の検索欄と `/admin/deleted` は従来からコードで引ける。データ修正は不要 |
| T-0079 | 2026-08-08 | ディールの新規作成でパイプラインを選ばせない | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の指摘「セールスを追加する際にパイプラインが選べる状態になっている。各パイプラインから作成するので選択肢は不要」。`/deals/new?pipeline=<screen_key>` で決まる形にし、選択欄は「セールス（営業）として作成します」の 1 行に置き換えた。指定が無ければ `pipeline_types.is_default`。**副次的に行き止まりが 1 つ見つかった**: リード必須が画面・Zod・RPC の 3 段でセールス前提のまま固定されており、**プロキュアメント・パートナーシップのディールが 1 件も作れなかった**（画面を分けるまで /deals/new は営業しか通らなかったので露見していなかった）。要否の正本を `pipeline_types.requires_lead` に寄せ、Zod に `lead_mode: "none"`、RPC は `requires_lead` を引いて判定するようにした。E2E-18 に 2 本目を足して守る |
| T-0080 | 2026-08-08 | 「商談」を「ディール」へ改称する | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の指摘「「商談」というテキストがまだ残っている。システム全体で統一してほしい」。パイプラインをセールス / プロキュアメント / パートナーシップに分けた結果、仕入れ先・委託先との取引まで「商談」と呼ぶのが実態に合わなくなった。**内部名は `deal` のまま**、画面・トースト・エラー文言・DB 例外文言・設計書を「ディール」に統一（src 353 / e2e 101 / docs 518 箇所）。**マスタの実値は変えない**（リードステータス「商談化」、営業パイプラインのステージ「商談」は本番で運用者が付けた名前）。台帳（tasks.md / test-checklist.md）は当時の記録なので置換対象外 |
| T-0081 | 2026-08-08 | 新規作成の担当者をログイン中の利用者にする | 依頼 | 低 | 利用者 | 完了 | 済 | 2026-08-09 | リードだけが自分を既定にしており、ディール・取引先・事業者情報・連絡先・プロジェクトは空欄だった。自分が起票したものを自分に付けるのが大半で、毎回選ばせると付け忘れが出る。5 画面に `defaultOwnerUserId` を渡して揃えた |
| T-0078 | 2026-08-08 | ステージ・ステータスのマスタが本番とローカルで食い違っていた | 不具合 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の依頼「ステージやステータスの設定を本番と合わせてください」。本番の 6 マスタを書き出して 1 行ずつ照合したところ、**seed が本番より遅れていた**。①リードステージの名称と色が全部違う（獲得→リード獲得 / 育成→ナーチャリング / 選定→リード選定 / Sales→ディール / Opportunity→オポチュニティ / Dead→デッド）②`call_scheduled`（架電予定）は本番で廃止済みなのに seed に残っていた ③`appointment_obtained` が「アポ獲得」→「見込み判断中」に改称され定義文も付いていた ④`opt_out` が「DM/TLオプトアウト」に改称され**デッドからナーチャリングへ移っていた** ⑤`card_exchanged` の UUID が本番と別（本番は管理画面で採番された `7d779305-…`）⑥`lead_categories` が `Inquiry`→`INQUIRY`、4 件とも定義文あり ⑦`pipeline_types.default_close_months` がローカルだけ NULL（`20260730000005` の UPDATE が `db reset` では seed より前に走って空振りする。T-0053 の型）。**本番を正本として seed を合わせた。** あわせて**利用者に見える文言からステージ名を外した**（`「選定」以上へ進めてください` → `商談を作れる段階まで進めてください`）。管理画面で改名できるものを文言に埋め込むと同じ食い違いが再発する。**本番の業務委託には手作りの「契約」ステージ（表示順 0）とステータス「手続き」があった。** 利用者判断で拾い上げる形にし、`ensure_pipeline_stages()` は UUID 決め打ちをやめて**パイプライン + 名前**で引くようにした（表示順は 0 のときだけ入れる／既にステータスが置かれているステージには足さない）。本番状態を再現して 1 回目・2 回目とも意図どおりになることを確認済み |
| T-0077 | 2026-08-08 | 「リードカテゴリ」を「デマンドファネル」へ改称する | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の意図は**意味づけの明確化**。「本来はセールスファネル（TQL・SQL）とマーケティングファネル（Inquiry・MQL）に分けるべきだが、詳細化しすぎるので包括的なデマンドファネルとする」との判断。**変えるのは表示ラベルだけ**（`lead_categories` のテーブル名・`code`・`resolve_lead_category` などの識別子は変えない。CLAUDE.md「コードは変更しない、名前変更は可」）。Phase 1 で商談の判定を `lead_stages.is_deal_ready` に寄せてあるので、改称の影響は表示に閉じる。あわせて**リード編集画面に残っているカテゴリの選択欄を読み取り専用にする**（新規作成は T-0072 で対応済み。`trg_leads_set_category` が保存のたび上書きする導出値で、選んでも効かない） |
| T-0073 | 2026-08-08 | パイプラインごとに画面を分ける（セールス / プロキュアメント / パートナーシップ） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の判断「商談と仕入れ・業務委託は性質が異なる」。`/sales` `/procurement` `/partnership` のトップレベル 3 画面にし、`/deals` は `/sales` へ逃がす。**商談の詳細（`/deals/{id}`）は分けない**（分けるとリンク元＝契約・プロジェクト・リード・横断検索・活動履歴が全部パイプラインを知る必要が出る）。前例は `/progress/*`（`_category.ts` + 共有 Workspace + 各 13 行の page.tsx）。画面とパイプラインの対応は **`pipeline_types.screen_key`** で持つ（`lead_categories.progress_view` と同じ形。slug は自動採番になり「引くな」とされている） |
| T-0074 | 2026-08-08 | 仕入れ・業務委託のパイプラインが空で使えない | 不具合 | 高 | 自主 | 完了 | 済 | 2026-08-09 | **ステージもステータスも 0 件**（seed にあるのは営業だけ）。選ぶとカンバンが列ゼロになり、商談も作れない（ステージ・ステータスが必須のため）。T-0073 で画面を分ける前提として埋める。**マイグレーションと seed の両方に書く**（seed は `db reset` でしか走らず、本番はマイグレーションでしか当たらない。T-0053 の型） |
| T-0075 | 2026-08-08 | ダッシュボードのファネルがパイプライン混在で壊れる | 不具合 | 高 | 自主 | 完了 | 済 | 2026-08-09 | `dashboard/page.tsx` が `deal_stages` を**パイプライン無関係に全件**取得して並べている。営業しかステージが無い今は偶然動いているだけで、**T-0074 で仕入れ・業務委託を seed した瞬間に同名の列が重複した壊れたファネルになる**。KPI（進行中商談数・合計金額・今月クローズ）も全パイプライン合算 |
| T-0076 | 2026-08-08 | 商談一覧のテーブルがパイプラインで絞られていない | 不具合 | 中 | 自主 | 完了 | 済 | 2026-08-09 | カンバンは `getDealsForKanban(pipelineId)` で絞るのに、テーブルは `getDeals()` に `pipelineTypeId` を渡していない（引数は存在する）。一方フィルタの候補（ステージ・ステータス）は選択中のパイプラインで絞られており、**「候補は営業のステージだけ、行は全パイプライン」という食い違い**が起きている |
| T-0069 | 2026-08-08 | 商談にリードを必ず紐づける（DB） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | **`deals` に `lead_id` が無く、商談側からリードを辿れない**（逆引きは `leads.promoted_deal_id` の 1 本だけで、1 リード 1 商談しか表せない）。`deals.lead_id` を新設して**紐づけの正本**にし、`promoted_deal_id` は「最初に紐づいた商談」の派生値へ降格（トリガーが維持。**列は落とさない**。撤去すると DB 6 個 + UI 3 箇所 + E2E 4 本に波及する）。必須条件は `pipeline_types.requires_lead`、「TQL 以上」は `lead_stages.is_deal_ready` で表す（**「TQL」「営業」という語を DB に入れない**ので Phase 2・3 から独立する）。値は `apply_master_role_flags()` に足す（code/slug の名指しを許された唯一の場所。`db reset` でも本番でも当たる。T-0053 の型）。**既存商談は遡って埋めず止めもしない**（grandfathering。全商談に必須を効かせると既存の編集が全部止まる）。検出は `v_deals_without_lead`。**実装中に踏んだ**: 段階（`is_deal_ready`）の検査をトリガーに置くと**昇格が壊れる**（昇格は「商談を作ってからステージを上げる」順序で動くため、途中ではリードが獲得のまま）。検査は `create_deal_with_lead`（新規作成画面が通る経路）へ移した |
| T-0070 | 2026-08-08 | 商談の新規作成をリード起点にする（画面） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 利用者の指摘「商談に取引先は新規作成時点では紐づかないのに設定できるのはなぜか」から。**T-0064 で私が残したもの**で、DB 制約に合わせただけで業務の流れとして筋が通っていなかった。**取引先の欄を新規作成から外し**、最上部にリードの欄を置く（既存リードを選ぶ / インラインで新規作成）。リードを選ぶと事業者情報・連絡先を自動補完（**変更可**。リードに `company_id` が無いケースが多く固定すると詰む）。**TQL 未満は選ばせないのではなく、その場で上げる**（チェックボックスで明示同意。黙って上げない）。リードの新規作成を `/leads/new` へ飛ばさないのは商談の入力が消えるため。**`updateLead` の `willPromote` を `deals.lead_id` 基準に直す**（直さないと `/deals/new` で作った商談があるリードを Sales へ上げたとき 2 本目が自動生成される） |
| T-0071 | 2026-08-08 | 昇格で既存の事業者情報へ寄せる | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | **`src/actions/leads.ts:755-770` の法人番号チェックが昇格を丸ごと拒否していた。** リードに既に事業者が紐づいていても見ずに弾く（そもそも SELECT に `company_id` が入っていない）。**1:N を否定する文言なので削除**し、名寄せに任せる。あわせて `promote_lead_to_deal` の Company 作成を `resolve_or_create_company()` 経由にする（**昇格経路だけが名寄せを通らず無条件 INSERT していた**。取込経路は既に再利用できている）。名寄せが受け取らない項目（カナ・代表者名・ステータス）は**空欄だけ COALESCE 補完**し、既存の値を上書きしない |
| T-0072 | 2026-08-08 | リードと事業者情報の 1:N を画面から扱えるようにする | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-09 | **DB 上は既に「事業者 1 : リード N」**（制約は無い）だが、運用上まったく見えない。①事業者情報の詳細に**リード一覧セクション**が無い ②`getLeads` に `company_id` フィルタが無い ③**手動作成のリードは `company_id` を設定する手段が UI にも Zod にも無い**（`company_name` テキストだけ）。この 3 つを埋める。あわせてリードの新規作成・編集に残っている**カテゴリの select を読み取り専用に**する（`trg_leads_set_category` が保存のたび上書きする完全な導出値で、選んでも効かない） |
| T-0065 | 2026-08-08 | 他の商談に紐づいている契約を、商談から紐づけできてしまう | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | T-0063 で入れた「既存の契約を紐づける」は、`contracts.deal_id` が NOT NULL のため**必ず他の商談から奪う付け替え**だった。**利用者判断で `deal_id` を NULL 許容にし、候補を未紐づけの契約だけにする**。`listLinkableContracts` の `.neq("deal_id", dealId)` は SQL の NULL 比較で NULL 行を落とすので `.is("deal_id", null)` へ。1 商談に複数契約が下がる作りは維持。**新たに開く穴を 2 つ塞ぐ**: ①`ensure_account_on_contract` は AFTER INSERT のみで後から紐づけても取引先ができない → `AFTER UPDATE OF deal_id` を追加 ②`check_contract_deletion_against_leads` は `BEFORE UPDATE OF deleted_at` なので、契約を消さずに剥がすとリードのステージ要件を素通りする → `deal_id` も監視対象に |
| T-0066 | 2026-08-08 | 商談の追加・編集画面で契約セクションがボタンとかぶる | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-09 | 編集画面の契約セクションが `</form>` の直後・フッター（削除／キャンセル／保存）のすぐ下にあり、ボタン行と接して見えた。`form-footer` は `margin-top: 1rem` のみで上に余白が無いのが根本。**フォームの中（日程カードの後・フッターの前）へ移し**、共通の余白を `1.5rem` に広げる。セクション内のボタンは全て `type="button"` なので submit されない。**「保存を押さずにすぐ反映される」ことを 3 つの手掛かりで伝える**（アクセント罫・「すぐ反映」バッジ・1 行の説明） |
| T-0067 | 2026-08-08 | 紐づけた契約を商談から解除できない | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | T-0065 の `deal_id` NULL 許容が前提。商談の編集画面の契約セクションと、契約詳細の商談欄の両方から外せるようにする。**契約そのものは残す**（削除ではない）。誤操作しにくくするため 4 段構え: 控えめなボタン（赤にしない。削除と取り違えさせない）・確認ダイアログ・削除と色を分ける・楽観ロック。`unlinkContractFromDeal` は manager 以上で、`deal_id` も受け取って「いま本当にこの商談に付いているか」を突き合わせる |
| T-0068 | 2026-08-08 | 契約名が任意入力のままで、命名が揃わない | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | **`締結日_契約書名_契約種別_金額_契約ID` を保存時に自動生成**する。人が入れる `contract_name`（契約書名）はそのまま残し、生成結果は新列 `contract_display_name` に入れる（既存の入力値を消さない）。**金額は `contracts.amount` を新設**（`deals.amount` は 1 商談に複数契約が下がるため使えない）。**生成は DB トリガー**（`contract_code` が BEFORE INSERT でしか確定せず、TS でやると 2 段書き込みになる。契約種別名も別テーブルにあり、マスタ名の変更に追随できない）。**TS 側に規則を二重実装しない**（`company-name.ts` で経験した「片方だけ直す」事故を避ける）。欠けた部品は落として連結（`__` を作らない）。一覧の 1 列目・詳細の見出し・横断検索・変更履歴の対象名を自動生成名にする。**変更履歴には自動生成列の差分を残さない**（材料を直すたび 2 行出るため。前例 `20260728000003`） |
| T-0063 | 2026-08-07 | 商談フォームの「契約名」が契約テーブルと二重管理になっている | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 商談の新規作成・編集にある「契約名」は `deals.contract_name`（ただのテキスト列）で、`contracts` テーブルとは繋がっていなかった。**利用者判断で契約テーブルへ一本化**。入力欄を両画面から外し、編集画面に契約セクション（一覧・新規作成・既存の紐づけ）を置いた。**`contracts.deal_id` は NOT NULL なので商談が保存されるまで契約は作れない**ため、新規作成画面は案内だけにしてある。**「紐づける」は付け替えで、元の商談からは外れる**（`deal_id` は 1 本しかない）。候補一覧に移動元を必ず出し、楽観ロックを**必須**にした（候補を開いたまま放置すると後勝ちで別の商談から契約を奪う）。取引先を作る `ensure_account_on_contract` は AFTER INSERT なので**付け替えでは走らない**。列は落とさず非推奨の COMMENT を付けた（`20260807000001`。値は人が入れた記録で、機械的に contracts へ移すと契約成立の判断を代行してしまう）。E2E-17・総ざらいで固定。`database-design.md §16.6.1` |
| T-0064 | 2026-08-07 | 商談の相手先が排他で「Ａ社のＢさん」を表せない | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-09 | 新規作成の相手先がラジオの 3 択で **1 つしか選べず**、DB の `deals_counterparty_check`（いずれか 1 つ以上）より狭かった。排他をやめ、事業者情報・連絡先・取引先をそれぞれ選べる形にした。**実際に操作して詳細ページの取りこぼしを発見**（両方紐づけても表示は 1 つだけだった。`getDealCounterparty` が優先順位で 1 件しか返さないため）。詳細用に `getDealCounterparties` を足して両方出す。一覧・カンバンは 1 行で示す場所なので単数のまま。社内の担当者と紛れないよう欄名を「担当者（社内）」「連絡先（先方の担当者）」に分けた。UT-73・E2E-17・総ざらいで固定 |
| T-0021 | 2026-08-04 11:10 | freee 会計の取引先を CRM と連携する（freee→CRM 読み取り専用） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `6bd527e` ほか。本番反映済み。**freee の接続と定期同期の登録は T-0036 へ** |
| T-0023 | 2026-08-04 13:10 | リードのステージと実体（商談・契約）の整合を規則として強制する | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `eeccb90`。ステージ要件を DB トリガーで強制。本番反映済み |
| T-0024 | 2026-08-04 15:10 | 連絡先の新規作成にメール・電話・住所を入れ、編集画面と揃える | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `f9fdfd5`。連絡先の作成を DB 関数で単一トランザクション化。本番反映済み |
| T-0025 | 2026-08-04 15:10 | リレーションのある詳細画面から子を追加できるようにする | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `f9fdfd5`。8 経路の追加導線。本番反映済み |
| T-0027 | 2026-08-04 16:00 | 商談の新規作成が「取引先」しか相手にできない | 不具合 | 高 | 自主 | 完了 | 済 | 2026-08-05 02:10 | `f9fdfd5`。相手先を 3 択に。本番反映済み |
| T-0028 | 2026-08-04 16:00 | タレントの新規作成画面を作る | 不具合 | 中 | 自主 | 完了 | 済 | 2026-08-05 02:10 | `f9fdfd5`。`/talents/new` を新設。本番反映済み |
| T-0030 | 2026-08-04 17:30 | 代表者名を連絡先（法人代表）から選べるようにする | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `0859eee`。代表者を連絡先から選択。本番反映済み |
| T-0031 | 2026-08-04 17:30 | 事業者情報・取引先のステータスを自動付与にする | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `3588e46`。契約とリードから自動判定。本番反映済み |
| T-0032 | 2026-08-04 17:30 | 取引先に「担当者情報」「請求者情報」セクションを作る | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `3588e46`。担当者情報・請求者情報。本番反映済み |
| T-0033 | 2026-08-04 17:30 | freee との相互同期（CRM→freee の新規作成・更新） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `ab897c3` ほか。差分を確認して反映。連携項目を 9 → 20 以上へ。本番反映済み |
| T-0034 | 2026-08-04 17:30 | 事業者情報が個人のとき法人向け項目を隠す | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `0859eee` / `9618830`。個人事業主の項目整理。本番反映済み |
| T-0035 | 2026-08-04 17:30 | 住所登録で郵便番号から住所を検索できるようにする | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `0859eee`。郵便番号から住所検索。本番反映済み |
| T-0001 | 2026-08-04 01:10 | 名刺取込の statement timeout を解消する | 不具合 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `d47e4f2`。本番 DB 適用済み・イメージ反映済み |
| T-0002 | 2026-08-04 01:10 | エラー文言の日本語化を全 Server Action に行き渡らせる | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `bf2d75f`。31 ファイル 192 箇所。本番反映済み |
| T-0019 | 2026-08-04 01:45 | 名刺取込をジョブ方式（非同期）に切り替える | 改善 | 高 | 利用者 | 完了 | 済 | 2026-08-05 02:10 | `f9ccd1e`。ジョブ方式。本番反映済み |
| T-0003 | 2026-08-04 01:10 | タスク管理体制を作る | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-04 01:17 | 本書 + `docs/task-management.md` |
| T-0015 | 2026-08-04 00:20 | 一覧の条件を続けて変えると消える問題を直す | 不具合 | 高 | Gate 3 | 完了 | 済 | 2026-08-04 01:05 | `faef69b`。`test-checklist.md` の 2026-08-04 節 |
| T-0016 | 2026-08-04 00:15 | ワークツリーを lint / テストの対象から外す | 改善 | 中 | Gate 1 | 完了 | 済 | 2026-08-04 00:48 | `5a84883` |
| T-0017 | 2026-08-03 12:00 | 本番へ migration 10 本を適用する | 運用 | 高 | 自主 | 完了 | 済 | 2026-08-04 01:00 | ローカル 128 / リモート 128、未適用 0。`test-checklist.md` の本番反映の記録 |
| T-0038 | 2026-08-05 12:30 | freee 連携で取引先コードの更新ができない | 不具合 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | freee の更新 API に `code` が無く 400「このAPIでは code の指定はできません。」で**その回の全項目**が落ちていた。更新用の型から外し、画面・Server Action・DB 関数の 3 層で弾く。組み立てを `src/lib/freee/payload.ts` へ切り出して UT-64 を追加。本番反映済み（マイグレーション `20260805000014`）。**freee 接続後の実機確認は T-0036 へ** |
| T-0039 | 2026-08-05 12:30 | freee への新規登録（`createPartner`）が未実装のまま | 改善 | 中 | 自主 | 完了 | 済 | 2026-08-05 21:40 | T-0041 で実装。`createFreeePartnerForCompany` から呼ぶ。T-0041 とともに本番反映済み |
| T-0041 | 2026-08-05 13:10 | 連携する事業者を追加する（CRM → freee の新規登録）と取引先コードでの名寄せ | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | `/admin/freee/register` を新設。似た取引先を確認してから登録し、取引先コードに事業者コードを入れる。自動紐付けを**コード → インボイス番号 → 法人番号**の順へ。マイグレーション `20260805000015`。ローカルで DB 関数 5 本と画面を検証済み（`test-checklist.md`）。本番反映済み。**実際の登録は freee 接続（T-0036）の後** |
| T-0042 | 2026-08-05 14:00 | 検索欄で日本語入力の変換中に検索が走る（**再発**） | 不具合 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | **同じ指摘を繰り返し受けていた。** 画面ごとに書いていた検索欄の処理を `useSearchField` へ寄せ、判断を `src/lib/search-field.ts`（純粋関数）に切り出して UT-66 と E2E-14 で固定。対象は一覧の検索欄・横断検索・検索付き選択欄・キャンペーン詳細の 4 経路。**修正前の実装で E2E-14 が落ちることも確認済み**。本番反映済み。**日本語入力の実機確認は T-0037 へ** |
| T-0043 | 2026-08-05 14:25 | freee の名称・カナの入り先を揃え、敬称の既定を「様」にする | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | 会社名を `name`（基本情報の名前）と `long_name`（書類の正式名称）の**両方**で比較するよう差分検出を修正（送信は元から両方だったが、検出が `COALESCE` で片方しか見ておらず正式名称が空のまま残っていた）。敬称は未設定のときだけ「様」を提案し、新規登録は常に「様」。マイグレーション `20260805000016`。**基本情報の「名前（ふりがな）」は API に項目が無く設定できない**（T-0044 へ）。本番反映済み |
| T-0045 | 2026-08-05 15:00 | 連絡先を Google コンタクトへ同期する（Phase 1: 接続 + CRM→Google） | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | 設計書 `docs/google-contacts-sync.md`。接続（OAuth + ドメイン検証）・push（作成/更新/削除）・グループ管理・定期同期まで実装。マイグレーション `20260805000017`。UT-67〜69 と総ざらいで確認済み。**Gmail と同じ GCP プロジェクトへの OAuth クライアント追加と環境変数 5 つ**が有効化に必要（§8.2）。本番反映済み。**有効化の運用作業（OAuth クライアント・環境変数・利用者ごとの接続・定期同期の登録）は T-0055 へ** |
| T-0047 | 2026-08-05 18:10 | マスタのスラッグ／コードを自動採番にし、画面から入力欄を外す | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | **そのままランダム化すると静かに壊れる箇所が 6 つあった**（`slug='generation'` で既定ステージを引く等。該当なしで NULL になりエラーが出ない）。先に意味のある列（`is_inquiry_default` / `requires_corporate_fields` / `is_default` / `progress_view`）へ移してから自動採番にした。マイグレーション `20260805000018`・`000019`。E2E 10 本と総ざらいで回帰確認済み。本番反映済み。Gate 5 で役割フラグ 19 個がすべて 1 であることを確認 |
| T-0049 | 2026-08-05 19:00 | スラッグ依存の見落とし 2 件を直し、マスタの手動設定を DB で守る | 不具合 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | **T-0047 で「参照を全部なくした」と報告したのは誤りだった。** DB 関数の中を見落としており、`resolve_lead_category`（新ステージが必ず MQL になる）と `ensure_account_on_contract`（取引先の種別が空になる）が残っていた。どちらも例外を出さず静かに壊れる。併せて `is_system_required` と使用中チェックで削除を拒む。マイグレーション `20260805000020`。E2E 10 本＋総ざらいで確認。本番反映済み |
| T-0050 | 2026-08-05 19:30 | マスタを名指ししている箇所を全廃し、役割フラグへ移す | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | 全体を機械的に洗って **15 箇所**検出（UUID 直書き 3・DB 関数 8・アプリ 4）。役割フラグへ移し、削除保護と「削除済みマスタを参照させない」トリガーを追加。マイグレーション `20260805000021`〜`000023`。**検査手順を `database-design.md §23.8.2` に残した**（DB 関数の本文まで検索する。これを怠って 2 回見落とした）。本番反映済み。Gate 5 で役割フラグ 19 個がすべて 1 であることを確認 |
| T-0051 | 2026-08-05 19:30 | 事業者情報 27 件が削除済みステータスを指していた（**データ破損**） | 不具合 | 高 | 自主 | 完了 | 済 | 2026-08-05 21:40 | 2026-07-31 の実在性ステータス移行後も、アプリの UUID 直書き（`COMPANY_STATUS_ACTIVE`）が削除済みの行を指す新規データを作り続けていた（8/4〜8/5 の 27 件）。**論理削除は外部キーで防げないため無症状**。`20260805000023` で修復し、再発防止のトリガーを追加。**本番にも同じ破損がある可能性が高い**ので、反映後に件数を確認すること。本番反映済み。**本番の該当は 0 件**（`20260805000023` の自動修復が走った後の確認） |
| T-0052 | 2026-08-05 19:50 | 変更履歴の取りこぼしと英語表示を直す | 依頼 | 高 | 利用者 | 完了 | 済 | 2026-08-05 21:40 | **記録対象が 9 テーブルだけでマスタが 1 つも記録されていなかった**（76 へ拡大）。表示は DB のカラム名・UUID・ISO 日時が生のまま出ており、作成時は全カラムの JSON が 1 セルに入っていた。`src/lib/change-log-format.ts` に変換を集約（UT-70）。論理削除を `SOFT_DELETE` として記録し、対象名（`_name`）も残す。過去分も移行済み。マイグレーション `20260805000024`〜`000026`。本番反映済み |
| T-0053 | 2026-08-05 20:10 | まっさらな DB では役割フラグが 1 つも立たない（**ローカルと本番の食い違い**） | 不具合 | 高 | 自主 | 完了 | 済 | 2026-08-05 21:40 | 本番反映前の確認で `npx supabase db reset` を通したところ seed が失敗した。**`db reset` はマイグレーション → seed の順**なので、マイグレーション内の `UPDATE ... WHERE code='...'` は 行がまだ無く全部 0 行更新になる。エラーは出ない。つまり**ローカルで検証したつもりの DB は本番と別物**だった。フラグ設定を冪等な関数 `apply_master_role_flags()`（`20260805000027`）へ集約し、seed の末尾からも呼ぶ。**本番でフラグが空振りしていた場合の復旧手段も兼ねる**。併せて開発 seed に残っていたステータスの UUID 直書き 3 件を役割フラグ参照へ置換。本番反映済み。**本番では空振りしていなかった**（Gate 5 で 19 個すべて 1）。復旧手段としてはそのまま残る |
| T-0054 | 2026-08-05 21:40 | 本番のリード 8 件が削除済みステータス「アポ確定」を指していた | 不具合 | 高 | Gate 5 | 完了 | 済 | 2026-08-05 21:40 | 反映後の確認②で検出。`appointment_confirmed`（アポ確定）が使用中のままマスタから論理削除されており（`20260805000020` の使用中チェックを入れる前の操作）、2026-07-28 取込のリード 8 件が取り残されていた。**ステージ／ステータスは業務判断が入るため `20260805000023` の自動修復対象外**にしてある。利用者判断で `appointment_obtained`（アポ獲得）へ寄せた。併せて seed（`01-masters.sql` / `04-leads.sql`）・生成スクリプト・`enums.ts`・設計書から「アポ確定」を外し、**本番に無い値がローカルにだけ残る状態**（T-0053 と同型）を解消。本番の 8 件を付け替え、再確認で 0 件。seed・生成スクリプト・`enums.ts`・設計書からも「アポ確定」を外した |
| T-0062 | 2026-08-07 | シークレット台帳を全プロジェクト横断のグローバル管理へ移す | 依頼 | 中 | 利用者 | 完了 | 済 | 2026-08-07 | キー名 ↔ 転記先の台帳を `~/.claude/secrets/ledger/iterra-hub.md` へ移し、本リポジトリの `docs/secrets-management.md` は参照側の実装・移行チェックリスト・判断の記録だけにした。共通方針はグローバルスキル `secrets-management` へ昇格（`~/.claude/docs/secrets-policy.md` は削除）。**Bitwarden が Free プランで 3 枠しか持てない**実態に合わせ「1 リポジトリ = 1 プロジェクト」の規約を廃止した。台帳は `claude-project-manager` で版管理する。**アプリのコードには影響しない**（本リポジトリの変更は `CLAUDE.md` と `docs/secrets-management.md` の 2 ファイル）。移設前後でキー名の欠落が無いことを機械的に突き合わせて確認済み。Bitwarden 側の実物との突き合わせはユーザー作業として `~/.claude/secrets/INDEX.md` に残した |

## T-0019 の方針メモ（2026-08-04）

**現状**: 数千行の取込を 1 回の HTTP リクエスト内で処理している。設計として選んだ記録はなく、
制限に当たるたびに回避策を足してきた（`lead-import-eight.md` にも「service_role を使う」の
一文しかない）。**DB 側の時間制限を延ばしても、Supabase の HTTP 層のタイムアウトは残る。**

**この環境は非同期化に向いている**:

- アプリは NAS の Docker で**常駐**する。サーバーレスではないのでリクエストを跨いで処理を続けられる
- **pg_cron が既に稼働している**（90 日経過分の物理削除・週次のスコア再計算）

| 案 | 仕組み | 利点 | 欠点 |
|---|---|---|---|
| **A. ジョブテーブル + pg_cron** | 取込内容を `import_jobs` に投入し、pg_cron が拾う。画面は進捗をポーリング | ブラウザを閉じても進む。NAS 再起動にも耐える | 着手まで最大 1 分の遅延 |
| B. アプリ内バックグラウンド | Server Action は登録だけして即返し、常駐 Node が処理 | 遅延なし | コンテナ再起動で中断（ジョブに状態を持てば再開可） |
| C. 分割送信 | クライアントが 200 行ずつ複数回呼ぶ | 実装が最小 | **1 トランザクションでなくなり部分適用が起きる**。CLAUDE.md の「書き込みは DB 関数にまとめる」に反するため採らない |

**推奨は A**（A + B の併用も可。登録時に即起動し、取りこぼしを pg_cron が拾う）。

## 棚卸しの記録

| 日時 | 棚卸しの内容 |
|---|---|
| 2026-08-04 01:17 | 台帳を新設。既存の未完了事項を `deployment-nas.md` の残作業・メモリ・`test-strategy.md` のロードマップから拾って起票した（T-0006〜T-0014） |
| 2026-08-05 02:10 | 本番へマイグレーション 12 本とイメージ `df15a76` を反映し、**検証中だった 15 件を完了へ移した**。残りは freee の接続（T-0036）と実機確認（T-0037） |
| 2026-08-05 21:40 | 本番へマイグレーション 14 本（`20260805000014`〜`000027`）とイメージ `ef7e4c3` を反映し、**検証中だった 13 件を完了へ移した**。Gate 5 で役割フラグ 19 個がすべて 1、破損データ 0 件を確認。反映後の確認で T-0054（削除済みステータスを指すリード 8 件）を検出し、同日中に処置。ID の無い壊れた行が 1 行あったので削除した。残る運用作業は freee の接続（T-0036）・Google コンタクトの有効化（T-0055）・実機確認（T-0037） |
| 2026-08-04 12:40 | freee 連携の実装再開時に、マイグレーションがローカル DB へ**手で流されただけで履歴に残っていない**状態を検出。DROP して `migration up` で正規に適用し直した。あわせて権限チェックの穴（`is_admin()` の NULL 伝播）を検出・修正。マニュアル画面の未反映を T-0022 として起票 |
