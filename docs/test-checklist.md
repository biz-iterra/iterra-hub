# ITERRA CRM テストチェックリスト

## 実行環境

| 項目 | 値 |
|---|---|
| テスト日 | 2026-04-16 |
| テストツール | Playwright (headless Chromium) |
| 解像度 | 1440 x 900 |
| ローカル Supabase | ポート 54331（iterra-hub 用） |

### テストユーザー

| メールアドレス | パスワード | ロール |
|---|---|---|
| admin@iterra.jp | password123 | admin |
| manager@iterra.jp | password123 | manager |
| member@iterra.jp | password123 | member |

### 前提条件

```bash
# 1. Supabase ローカル起動
supabase start

# 2. DB リセット（マイグレーション + シードデータ適用）
supabase db reset

# 3. 開発サーバー起動
npm run dev
```

---

## 1. ビルド・型チェック

### 1.1 TypeScript 型チェック

**手順:**
```bash
npx tsc --noEmit
```

**期待結果:** エラー 0 件で完了する

| 結果 | 備考 |
|---|---|
| OK | エラー 0 件 |

### 1.2 プロダクションビルド

**手順:**
```bash
npm run build
```

**期待結果:** 全ルートが正常に生成される（/, /dashboard, /deals, /contacts, /companies, /accounts, /contracts, /talents, /admin, /login）

| 結果 | 備考 |
|---|---|
| OK | 全 10 ルート生成 |

### 1.3 ESLint 静的解析

**手順:**
```bash
npm run lint
```

**期待結果:** エラー 0 件

| 結果 | 備考 |
|---|---|
| — | 未実行 |

---

## 2. DB マイグレーション・シードデータ

### 2.1 マイグレーション全適用

**手順:**
```bash
supabase db reset
```

**期待結果:** 全 13 マイグレーションファイルがエラーなく適用され、シードデータも投入される

**確認するログ出力:**
```
Applying migration 20260416040001_create_master_tables.sql...
Applying migration 20260416040002_create_skill_masters.sql...
...（中略）...
Applying migration 20260416040013_create_functions_triggers_rls.sql...
Seeding data from supabase/seed.sql...
Finished supabase db reset on branch main.
```

| 結果 | 備考 |
|---|---|
| OK | エラーなし |

### 2.2 シードデータ件数確認

**手順:**
```bash
supabase db query "
SELECT 'pipeline_types' as tbl, count(*) as cnt FROM pipeline_types
UNION ALL SELECT 'crm_users', count(*) FROM crm_users
UNION ALL SELECT 'companies', count(*) FROM companies
UNION ALL SELECT 'accounts', count(*) FROM accounts
UNION ALL SELECT 'contacts', count(*) FROM contacts
UNION ALL SELECT 'deals', count(*) FROM deals
UNION ALL SELECT 'skills', count(*) FROM skills
UNION ALL SELECT 'deal_stages', count(*) FROM deal_stages
UNION ALL SELECT 'deal_statuses', count(*) FROM deal_statuses
"
```

**期待結果:**

| テーブル | 期待件数 | 結果 |
|---|---|---|
| pipeline_types | 3 | OK |
| deal_stages | 6 | OK |
| deal_statuses | 9 | OK |
| crm_users | 3 | OK |
| companies | 1 | OK |
| accounts | 1 | OK |
| contacts | 1 | OK |
| deals | 1 | OK |
| skills | 18 | OK |

### 2.3 自動採番トリガー確認

**手順:**
```bash
supabase db query "
SELECT company_code, account_code, contact_code, deal_code
FROM companies c, accounts a, contacts ct, deals d
WHERE c.id = '10000000-0000-0000-0000-000000000001'
  AND a.id = '20000000-0000-0000-0000-000000000001'
  AND ct.id = '30000000-0000-0000-0000-000000000001'
  AND d.id = '40000000-0000-0000-0000-000000000001'
"
```

**期待結果:**

| カラム | 期待値 | 結果 |
|---|---|---|
| company_code | CMP-000001 | OK |
| account_code | ACC-000001 | OK |
| contact_code | CNT-000001 | OK |
| deal_code | DL-000001 | OK |

---

## 3. 認証

### 3.1 ログインページ表示

**手順:**
1. ブラウザで http://localhost:3000 にアクセス

**期待結果:**
- /login にリダイレクトされる
- 「ITERRA CRM」タイトルと「営業・取引管理システム」サブテキストが表示
- メールアドレス入力欄・パスワード入力欄・「ログイン」ボタンが表示

| 結果 | 備考 |
|---|---|
| OK | |

### 3.2 admin ログイン

**手順:**
1. メールアドレス: `admin@iterra.jp` を入力
2. パスワード: `password123` を入力
3. 「ログイン」ボタンをクリック

**期待結果:**
- /dashboard にリダイレクトされる
- サイドバーに全メニュー（ダッシュボード〜管理）が表示される

| 結果 | 備考 |
|---|---|
| OK | /dashboard にリダイレクト |

### 3.3 セッション維持

**手順:**
1. ログイン状態で /deals に遷移
2. ブラウザのアドレスバーに http://localhost:3000/contacts を入力して Enter

**期待結果:** ログイン画面に戻されず、各ページが表示される

| 結果 | 備考 |
|---|---|
| OK | ページ遷移後も認証状態維持 |

### 3.4 未認証リダイレクト

**手順:**
1. ブラウザのシークレットウィンドウで http://localhost:3000/dashboard にアクセス

**期待結果:** /login にリダイレクトされる

| 結果 | 備考 |
|---|---|
| OK | middleware で制御 |

### 3.5 manager ログイン

**手順:**
1. ログアウト後、`manager@iterra.jp` / `password123` でログイン

**期待結果:**
- /dashboard にリダイレクトされる
- サイドバーに「管理」メニューが**表示されない**（admin のみ）

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 3.6 member ログイン

**手順:**
1. ログアウト後、`member@iterra.jp` / `password123` でログイン

**期待結果:**
- /dashboard にリダイレクトされる
- サイドバーに「管理」「契約」メニューが**表示されない**

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 3.7 不正パスワードでのエラー

**手順:**
1. メールアドレス: `admin@iterra.jp`
2. パスワード: `wrongpassword`
3. 「ログイン」ボタンをクリック

**期待結果:** 「メールアドレスまたはパスワードが正しくありません」エラーメッセージが赤字で表示される

| 結果 | 備考 |
|---|---|
| — | 未テスト |

---

## 4. Dashboard（/dashboard）

### 4.1 KPI カード表示

**手順:**
1. admin でログイン
2. /dashboard にアクセス

**期待結果:** 6 枚の KPI カードが 3 列グリッドで表示される

| KPI | 期待値 | 結果 |
|---|---|---|
| 進行中ディール | 1 / 「クローズ前のディール」 | OK |
| 進行中 合計金額 | ¥1,500,000 | OK |
| 今月クローズ | 0 / 「2026年4月」 | OK |
| アカウント数 | 1 / 「アクティブのみ」 | OK |
| コンタクト数 | 1 / 「アクティブのみ」 | OK |
| カンパニー数 | 1 / 「アクティブのみ」 | OK |

### 4.2 パイプラインファネル

**手順:**
1. Dashboard ページ下部の「パイプラインファネル」セクションを確認

**期待結果:**
- 全 6 ステージ（リード〜クローズ）が縦並びで表示
- 「商談」の横に「1件」とバーが表示される
- その他のステージは「0件」

| 結果 | 備考 |
|---|---|
| OK | |

### 4.3 最近のディール

**手順:**
1. 「最近のディール」セクションのテーブルを確認

**期待結果:**

| コード | 名前 | ステージ | 金額 | 担当者 |
|---|---|---|---|---|
| DL-000001 | サンプル案件 - Web制作 | 商談 | ¥1,500,000 | メンバーテスト |

| 結果 | 備考 |
|---|---|
| OK | |

### 4.4 最近の対応履歴

**手順:**
1. 「最近の対応履歴」セクションを確認

**期待結果:** シードデータに対応履歴がないため、空状態または「データなし」表示

| 結果 | 備考 |
|---|---|
| OK | データなし表示 |

### 4.5 担当者別ディール数

**手順:**
1. 「担当者別ディール数」セクションを確認

**期待結果:** メンバーテスト: 1件 が表示される

| 結果 | 備考 |
|---|---|
| OK | |

---

## 5. Deals（/deals）

### 5.1 カンバンビュー表示

**手順:**
1. サイドバーの「ディール」をクリック
2. 「カンバン」タブがアクティブであることを確認

**期待結果:**
- パイプライン選択ドロップダウンに「営業」が表示
- ステージカラムが横並びで表示（リード・商談・提案・見積り・交渉・クローズ）
- 各カラムヘッダーにステージ名とディール数バッジ

| 結果 | 備考 |
|---|---|
| OK | |

### 5.2 ディールカード内容

**手順:**
1. 「商談」カラム内のディールカードを確認

**期待結果:**
- カード左ボーダー: オレンジ系（soleil 色）
- ディール名: 「サンプル案件 - Web制作」
- 金額: ¥1,500,000
- アカウント名: 株式会社サンプル
- 担当者名: メンバーテスト

| 結果 | 備考 |
|---|---|
| OK | |

### 5.3 テーブルビュー切り替え

**手順:**
1. 「テーブル」タブをクリック

**期待結果:**
- テーブルヘッダー: ディールコード、取引名、ステージ、ステータス、金額、アカウント、担当者、作成日
- 1 行表示: DL-000001 / サンプル案件 - Web制作 / 商談 / 進行中 / ¥1,500,000 / 株式会社サンプル / メンバーテスト / 2026/4/16
- 検索バーが表示される

| 結果 | 備考 |
|---|---|
| — | 未テスト（クリック操作未実行） |

### 5.4 パイプライン切り替え

**手順:**
1. パイプライン選択ドロップダウンで「仕入れ」を選択

**期待結果:**
- カンバンのステージカラムが仕入れパイプラインのステージに切り替わる
- ディールが 0 件の場合は全カラムに「ディールなし」表示

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 5.5 新規作成ボタン

**手順:**
1. 右上の「+ 新規作成」ボタンの表示を確認

**期待結果:** terra 色の pill 型ボタンが表示される

| 結果 | 備考 |
|---|---|
| OK | 表示確認のみ |

---

## 6. Contacts（/contacts）

### 6.1 一覧テーブル表示

**手順:**
1. サイドバーの「コンタクト」をクリック

**期待結果:**
- ページタイトル「コンタクト」と「1 件のコンタクト」サブテキスト
- テーブルヘッダー: コード、氏名、種別、所属、メール、電話、ステータス、担当者

| 結果 | 備考 |
|---|---|
| OK | |

### 6.2 コンタクトデータ表示

**手順:**
1. テーブルの 1 行目の内容を確認

**期待結果:**

| カラム | 期待値 |
|---|---|
| コード | CNT-000001 |
| 氏名 | 山田 太郎 |
| 種別 | 「法人代表」バッジ（terra 背景・白文字） |
| 所属 | 株式会社サンプル |
| メール | yamada@sample.co.jp（Mail アイコン付き） |
| 電話 | 090-1234-5678（Phone アイコン付き） |
| ステータス | 「アクティブ」バッジ |
| 担当者 | メンバーテスト |

| 結果 | 備考 |
|---|---|
| OK | 全項目正常表示 |

### 6.3 種別バッジ色分け

**手順:**
1. 種別カラムのバッジの色を確認

**期待結果:**
- individual → sage 背景・白文字
- corporate_rep → terra 背景・白文字（今回のテストケース）
- employee → amber 背景・タイトル色文字
- other → デフォルト（sumi100）

| 結果 | 備考 |
|---|---|
| OK | corporate_rep のみ確認 |

### 6.4 検索

**手順:**
1. 検索バーに「山田」と入力
2. 300ms 後に結果が更新されることを確認

**期待結果:** 山田太郎の行のみ表示される

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 6.5 行クリック遷移

**手順:**
1. テーブルの山田太郎の行をクリック

**期待結果:** /contacts/[id] に遷移する（詳細ページは未実装のため 404 の可能性あり）

| 結果 | 備考 |
|---|---|
| — | 未テスト |

---

## 7. Companies（/companies）

### 7.1 一覧テーブル表示

**手順:**
1. サイドバーの「カンパニー」をクリック

**期待結果:**
- ページタイトル「カンパニー」
- テーブルヘッダー: 会社コード、会社名、事業者種別、代表電話、担当者、作成日

| 結果 | 備考 |
|---|---|
| OK | |

### 7.2 カンパニーデータ表示

**手順:**
1. テーブルの 1 行目の内容を確認

**期待結果:**

| カラム | 期待値 |
|---|---|
| 会社コード | CMP-000001 |
| 会社名 | 株式会社サンプル（太字） |
| 事業者種別 | 株式会社 |
| 代表電話 | 03-1234-5678 |
| 担当者 | マネージャーテスト |
| 作成日 | 2026/4/16 |

| 結果 | 備考 |
|---|---|
| OK | 代表電話が "-" 表示 → 要確認 |

### 7.3 ページネーション

**手順:**
1. テーブル下部のページネーションを確認

**期待結果:** 「1 / 1 ページ」表示、「前へ」「次へ」ボタンが非活性

| 結果 | 備考 |
|---|---|
| OK | |

### 7.4 検索

**手順:**
1. 検索バーに「サンプル」と入力
2. 結果が表示されることを確認
3. 検索バーをクリアし、全件に戻ることを確認

**期待結果:** 「株式会社サンプル」が表示される → クリア後に全件表示

| 結果 | 備考 |
|---|---|
| — | 未テスト |

---

## 8. Accounts（/accounts）

### 8.1 一覧テーブル表示

**手順:**
1. サイドバーの「アカウント」をクリック

**期待結果:**
- テーブルヘッダー: アカウントコード、アカウント名、カンパニー名、種別、ステータス、担当者、作成日

| 結果 | 備考 |
|---|---|
| OK | |

### 8.2 アカウントデータ表示

**手順:**
1. テーブルの 1 行目の内容を確認

**期待結果:**

| カラム | 期待値 |
|---|---|
| アカウントコード | ACC-000001 |
| アカウント名 | 株式会社サンプル（太字） |
| カンパニー名 | 株式会社サンプル |
| 種別 | -（account_type_id 未設定） |
| ステータス | 「アクティブ」バッジ |
| 担当者 | マネージャーテスト |
| 作成日 | 2026/4/16 |

| 結果 | 備考 |
|---|---|
| OK | |

### 8.3 ページネーション

**手順:** 7.3 と同様

| 結果 | 備考 |
|---|---|
| OK | 1 / 1 ページ |

---

## 9. Contracts（/contracts）

### 9.1 空状態表示

**手順:**
1. サイドバーの「契約」をクリック

**期待結果:**
- ページタイトル「契約」と「0 件の契約」サブテキスト
- FileText アイコン + 「契約がまだありません」メッセージが中央表示
- 検索バーと「+ 新規作成」ボタンが表示される

| 結果 | 備考 |
|---|---|
| OK | シードに契約データなし |

### 9.2 契約テーブル表示（データあり時）

**手順:**
1. Admin ページまたは Supabase Studio で契約データを追加
2. /contracts ページをリロード

**期待結果:**
- テーブルヘッダー: 契約コード、契約書名、ディール、契約種別、契約方法、契約開始日、契約終了日、登録者
- 契約方法バッジ: paper=デフォルト、electronic=sage 緑、verbal=amber 黄

| 結果 | 備考 |
|---|---|
| — | シードに契約データなし |

---

## 10. Talents（/talents）

### 10.1 空状態表示

**手順:**
1. サイドバーの「タレント」をクリック

**期待結果:**
- ページタイトル「タレント」と「0 件のタレント」サブテキスト
- UserCircle アイコン + 「タレントがまだありません」メッセージが中央表示

| 結果 | 備考 |
|---|---|
| OK | シードにタレントデータなし |

### 10.2 タレントテーブル表示（データあり時）

**手順:**
1. Supabase Studio で talents テーブルにデータを追加
2. /talents ページをリロード

**期待結果:**
- テーブルカラム: コンタクト名、部署・役職、スキル（上位 3 つバッジ）、総合評価（50 文字切り詰め）
- スキルバッジ: 「スキル名 Lv.X」形式、Lv.4 以上は sage 背景

| 結果 | 備考 |
|---|---|
| — | シードにタレントデータなし |

---

## 11. Admin（/admin）

### 11.1 ページ表示・タブ構成

**手順:**
1. サイドバーの「管理」をクリック

**期待結果:**
- ページタイトル「マスタ管理」
- 9 タブが横並び: パイプライン / 契約種別 / 事業者種別 / サービス / リードソース / アカウント種別 / アカウントステータス / コンタクトステータス / スキル
- 「パイプライン」タブがデフォルトでアクティブ（下線付き）

| 結果 | 備考 |
|---|---|
| OK | |

### 11.2 パイプラインタブ — 一覧表示

**手順:**
1. パイプラインタブを確認

**期待結果:**
- テーブルカラム: 名前、説明、表示順、操作
- 3 行表示: 営業（1）、仕入れ（2）、業務委託（3）
- 各行に「編集」「削除」ボタン
- テーブル右上に「追加」ボタン
- テーブル下に「パイプラインを選択してステージ・ステータスを管理」セレクトボックス

| 結果 | 備考 |
|---|---|
| OK | |

### 11.3 パイプラインタブ — ステージ・ステータス表示

**手順:**
1. パイプライン選択セレクトボックスで「営業」を選択

**期待結果:**
- 「ディールステージ」セクションが表示される
- 6 件のステージが表示（リード〜クローズ、sort_order 順）
- 「ディールステータス」セクションが表示される
- 9 件のステータスが表示

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 11.4 マスタ追加（モーダル）

**手順:**
1. 任意のタブ（例: 契約種別）を選択
2. 「追加」ボタンをクリック
3. モーダルが表示されることを確認
4. 名前フィールドに「テスト契約種別」と入力
5. 「保存」ボタンをクリック

**期待結果:**
- モーダル表示: オーバーレイ背景 + 白背景カード
- 保存後: モーダルが閉じ、テーブルに新しい行が追加される
- エラー時: 赤文字でエラーメッセージ表示

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 11.5 マスタ編集（モーダル）

**手順:**
1. テーブル内の任意の行の「編集」ボタンをクリック
2. モーダルに現在の値が入力された状態で表示されることを確認
3. 名前を変更して「保存」ボタンをクリック

**期待結果:**
- モーダルに既存データがプリフィルされている
- 保存後: テーブルの値が更新される

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 11.6 マスタ削除（確認モーダル）

**手順:**
1. テーブル内の任意の行の「削除」ボタンをクリック
2. 確認モーダルが表示されることを確認
3. 「削除」ボタンをクリック

**期待結果:**
- 確認モーダル: 「本当に削除しますか？」メッセージ + キャンセル/削除ボタン
- 削除後: テーブルから行が消える（論理削除: is_active=false）
- FK 参照がある場合: エラーメッセージ表示

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 11.7 各タブの切り替え

**手順:** 全 9 タブを順番にクリックし、各マスタのデータが表示されることを確認

| タブ | 期待件数 | 結果 |
|---|---|---|
| パイプライン | 3 | OK |
| 契約種別 | 4 | — |
| 事業者種別 | 6 | — |
| サービス | 5 | — |
| リードソース | 7 | — |
| アカウント種別 | 3 | — |
| アカウントステータス | 4 | — |
| コンタクトステータス | 4 | — |
| スキル | 6 カテゴリ / 18 スキル | — |

---

## 12. レイアウト・共通 UI

### 12.1 サイドバー

**手順:**
1. 各ページに遷移し、サイドバーの状態を確認

**期待結果:**
- terra 色（#3C3F58）の背景
- 「ITERRA CRM」ロゴ
- 全 8 メニュー項目（ダッシュボード〜管理）が表示
- 現在のページがハイライト（terra-dark 背景）
- 折りたたみボタン（左下 chevron アイコン）

| # | 確認項目 | 結果 |
|---|---|---|
| 12.1.1 | 全ナビゲーション項目表示 | OK |
| 12.1.2 | アクティブページハイライト | OK |
| 12.1.3 | 折りたたみボタン表示 | OK |
| 12.1.4 | 折りたたみ動作 | — 未テスト |

### 12.2 ヘッダー

**手順:**
1. 各ページのヘッダーを確認

**期待結果:**
- 左: ページ名（パンくず）
- 右: ユーザーアイコン + ユーザー名 + ログアウトボタン

| # | 確認項目 | 結果 |
|---|---|---|
| 12.2.1 | ページタイトル表示 | OK |
| 12.2.2 | ユーザー名表示 | OK |
| 12.2.3 | ログアウトボタン表示 | OK |
| 12.2.4 | ログアウト動作 | — 未テスト |

### 12.3 デザイントークン適用

**手順:** 各ページの UI 要素がデザインシステムに準拠しているか目視確認

| # | 確認項目 | 結果 |
|---|---|---|
| 12.3.1 | サイドバー terra 色 | OK |
| 12.3.2 | カード elevation-low シャドウ | OK |
| 12.3.3 | カード radius-card 角丸 | OK |
| 12.3.4 | ボタン radius-button（pill 型） | OK |
| 12.3.5 | テーブルヘッダー sumi50 背景 | OK |
| 12.3.6 | テーブル行 border-default 区切り | OK |

---

## 13. RLS ポリシー

### 13.1 admin — 全データ閲覧

**手順:**
1. admin@iterra.jp でログイン
2. 各ページでデータが全件表示されることを確認

**期待結果:** 全テーブルのデータが制限なく閲覧可能

| 結果 | 備考 |
|---|---|
| OK | Dashboard で全件取得確認 |

### 13.2 manager — 全データ閲覧・自担当編集

**手順:**
1. manager@iterra.jp でログイン
2. Companies ページで全件表示されることを確認
3. 契約ページにアクセスして閲覧できることを確認

**期待結果:** SELECT は全件可、UPDATE は自担当のみ、Contracts 閲覧可

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 13.3 member — 自担当のみ閲覧

**手順:**
1. member@iterra.jp でログイン
2. Companies ページで owner_user_id = member のデータのみ表示されることを確認
3. 契約ページにアクセスして閲覧**不可**であることを確認

**期待結果:** owner_user_id ベースでフィルタされる、Contracts は閲覧不可

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 13.4 admin — マスタ変更可能

**手順:**
1. admin@iterra.jp でログイン
2. Admin ページでマスタの追加・編集・削除が可能であることを確認

**期待結果:** 全マスタの CRUD が可能

| 結果 | 備考 |
|---|---|
| — | 未テスト |

### 13.5 member — マスタ変更不可

**手順:**
1. member@iterra.jp でログイン
2. Admin ページの操作でエラーが返ることを確認（UI 上はサイドバーに「管理」が表示されない前提）

**期待結果:** 「管理者権限が必要です」エラー

| 結果 | 備考 |
|---|---|
| — | 未テスト |

---

## 14. アクセス制御（セキュリティ）— 必須テスト

> **重要:** アクセス制御は多層防御で実装されている（Middleware + Server Action + RLS）。
> 新機能追加・既存機能変更時は必ずこのセクションのテストを実施すること。

### 14.1 UUID パラメータ検証

**手順:**
1. admin でログイン
2. ブラウザのアドレスバーに `http://localhost:3000/companies/invalid-id` を入力

**期待結果:** 「不正なパラメータです」メッセージ + 「カンパニー一覧へ戻る」リンク

| 対象ページ | テスト URL 例 | 結果 |
|---|---|---|
| /companies/[id] | `/companies/not-a-uuid` | — |
| /accounts/[id] | `/accounts/../../etc` | — |
| /contacts/[id] | `/contacts/abc` | — |
| /deals/[id] | `/deals/123` | — |
| /contracts/[id] | `/contracts/<script>` | — |
| /talents/[id] | `/talents/null` | — |

### 14.2 他人のデータへの直接 URL アクセス

**手順:**
1. member@iterra.jp でログイン（owner_user_id = member の ID）
2. Companies ページで自分の担当でないカンパニーの ID をアドレスバーに入力
   - サンプルデータの CMP-000001 は owner=manager なので `/companies/10000000-0000-0000-0000-000000000001` にアクセス

**期待結果:** RLS により「カンパニーが見つかりません」が表示される（データが漏洩しない）

| 対象 | テスト方法 | 期待結果 | 結果 |
|---|---|---|---|
| companies | member で manager の company にアクセス | 「見つかりません」表示 | — |
| accounts | member で manager の account にアクセス | 「見つかりません」表示 | — |
| contacts | member で manager の contact にアクセス | 「見つかりません」表示 | — |
| deals | member で manager の deal にアクセス | 「見つかりません」表示 | — |

### 14.3 論理削除の admin 制限

**手順:**
1. manager@iterra.jp でログイン
2. ブラウザの開発者ツール → コンソールで Server Action を直接呼び出し（または API テスト）
3. `deleteCompany("10000000-0000-0000-0000-000000000001")` を実行

**期待結果:** 「管理者権限が必要です」エラーが返る

| 対象 Action | テストユーザー | 期待結果 | 結果 |
|---|---|---|---|
| deleteCompany | manager | 「管理者権限が必要です」 | — |
| deleteAccount | manager | 「管理者権限が必要です」 | — |
| deleteContact | manager | 「管理者権限が必要です」 | — |
| deleteTalent | member | 「管理者権限が必要です」 | — |
| deleteContract | manager（admin ではない） | 「管理者権限が必要です」 | — |

### 14.4 更新のオーナー制限

**手順:**
1. member@iterra.jp でログイン
2. manager が担当するカンパニーの更新を試行

**期待結果:** 「このカンパニーを編集する権限がありません」エラーが返る

| 対象 Action | テストユーザー | 対象データ | 期待結果 | 結果 |
|---|---|---|---|---|
| updateCompany | member | manager の company | 「権限がありません」 | — |
| updateAccount | member | manager の account | 「権限がありません」 | — |
| updateContact | member | manager の contact | 「権限がありません」 | — |
| updateDeal | member | member の deal（OK） | 正常更新 | — |

### 14.5 contracts のロール制限

**手順:**
1. member@iterra.jp でログイン
2. /contracts ページにアクセス

**期待結果:** データが表示されない（RLS で manager/admin のみ）

| テスト | テストユーザー | 期待結果 | 結果 |
|---|---|---|---|
| contracts 一覧閲覧 | member | 0 件 or エラー | — |
| contracts 一覧閲覧 | manager | 正常表示 | — |
| createContract | member | エラー | — |
| createContract | manager | 正常作成 | — |

### 14.6 マスタ CRUD の admin 制限

**手順:**
1. member@iterra.jp でログイン
2. Admin ページ（/admin）にアクセス
3. マスタ追加を試行

**期待結果:** 「管理者権限が必要です」エラー

| テスト | テストユーザー | 期待結果 | 結果 |
|---|---|---|---|
| マスタ追加 | member | 「管理者権限が必要です」 | — |
| マスタ編集 | member | 「管理者権限が必要です」 | — |
| マスタ削除 | member | 「管理者権限が必要です」 | — |
| マスタ追加 | admin | 正常作成 | — |

### 14.7 従属テーブルのアクセス制御

**手順:**
1. member@iterra.jp でログイン
2. manager が担当するコンタクトのメールアドレスを取得しようとする

**期待結果:** RLS により取得できない（親コンタクトの owner_user_id で制限）

| テスト | テストユーザー | 対象データ | 期待結果 | 結果 |
|---|---|---|---|---|
| contact_emails 閲覧 | member | manager のコンタクトのメール | 取得不可 | — |
| contact_phones 閲覧 | member | manager のコンタクトの電話 | 取得不可 | — |
| talent_skills 閲覧 | member | manager のタレントのスキル | 取得不可 | — |
| deal_services 閲覧 | member | manager のディールのサービス | 取得不可 | — |

---

## 15. 未テスト項目（今後の実施推奨）

### 操作テスト

- [ ] 各ページの検索機能（入力 → 絞り込み → クリア）
- [ ] ページネーション操作（次へ → 前へ → ページ数表示更新）
- [ ] テーブル行クリック → 詳細ページ遷移
- [ ] Deals テーブルビュー切り替え + 検索
- [ ] Deals パイプライン切り替え操作
- [ ] Admin 全タブのマスタ追加・編集・削除
- [ ] Admin パイプラインタブのステージ/ステータス CRUD

### 新規作成フォーム

- [ ] Company 新規作成（全フィールド入力 → 保存 → 一覧に反映）
- [ ] Account 新規作成（Company 紐づけあり/なし）
- [ ] Contact 新規作成（contact_type ごとの分岐テスト）
- [ ] Deal 新規作成（pipeline/stage/status の連動選択）
- [ ] Contract 新規作成（deal 紐づけ必須）
- [ ] バリデーションエラー表示（必須項目未入力、文字数超過等）

### ロール別テスト

- [ ] manager でログイン → 権限に応じた表示確認
- [ ] member でログイン → owner_user_id ベースのフィルタ確認
- [ ] member で Admin ページへの直接アクセス制限
- [ ] member で Contracts ページへのアクセス制限

### エッジケース

- [ ] 大量データ（100 件以上）でのページネーション動作
- [ ] 長い文字列（200 文字の会社名等）の表示切り詰め
- [ ] バリデーションエラー時のフォーム表示・エラーメッセージ
- [ ] Supabase 接続エラー時の画面表示
- [ ] ログアウト → セッション切れ → 再ログインフロー

### レスポンシブ

- [ ] タブレット表示（768px）— サイドバー折りたたみ、テーブル横スクロール
- [ ] モバイル表示（375px）— サイドバーオーバーレイ、カード 1 列
- [ ] カンバンビューのモバイル表示（横スクロール確認）
